<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ril.cardmanager"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="13"
        android:targetSdkVersion="18" />

    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light" >
        <activity
            android:name="com.ril.cardmanager.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.ril.cardmanager.AddCard" >
        </activity>
        <activity android:name="com.ril.cardmanager.EditCard" >
        </activity>
        <activity android:name="com.ril.cardmanager.TeamManager" >
        </activity>
        <activity android:name="com.ril.cardmanager.LeaddingIn" >
        </activity>
        <activity android:name="com.ril.cardmanager.LeaddingOut" >
        </activity>
        <activity android:name="com.ril.cardmanager.ListTeamManager" >
        </activity>
        <activity android:name="com.ril.cardmanager.EditTeamManager" >
        </activity>
    </application>

</manifest>